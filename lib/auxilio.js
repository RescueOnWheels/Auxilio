const Debug = require('./debug');
const arraysEqual = require('./arraysEqual');
const isDuplicate = require('./isDuplicate');
const normalize = require('./normalize');
const coordinateCalcutator = require('./coordinateCalculator');
const toRadians = require('./toRadians');
const generateToken = require('./generateToken');

module.exports = {
  Debug,
  arraysEqual,
  isDuplicate,
  normalize,
  coordinateCalcutator,
  toRadians,
  generateToken,
  
};
